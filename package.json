{
  "name": "test",
  "version": "1.0.0",
  "description": "hz",
  "main": "index.js",
  "repository": "none",
  "author": "none",
  "license": "none",
  "scripts": {
    "test": "yarn hardhat test",
    "compile": "yarn hardhat compile",
    "coverage": "yarn hardhat clean; yarn hardhat coverage",
    "size": "yarn hardhat size-contracts",

    "verify": "yarn hardhat verify",
    "verify:test": "yarn hardhat verify --network sepolia",
    "verify:main": "yarn hardhat verify --network sepolia",

    "deploy": "yarn hardhat run scripts/deploy.ts",
    "deploy:test": "yarn hardhat run scripts/deploy.ts --network sepolia",
    "deploy:main": "yarn hardhat run scripts/deploy.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-chai-matchers": "^2.0.2",
    "@nomicfoundation/hardhat-ethers": "^3.0.4",
    "@nomicfoundation/hardhat-network-helpers": "^1.0.9",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@nomicfoundation/hardhat-verify": "^1.1.1",
    "@typechain/ethers-v6": "0.5.0",
    "@typechain/hardhat": "9.0.0",
    "@types/chai": "4.3.5",
    "@types/mocha": "10.0.1",
    "@types/node": "20.5.7",
    "chai": "^4.3.8",
    "dotenv": "^16.3.1",
    "ethers": "^6.7.1",
    "hardhat": "^2.17.2",
    "hardhat-gas-reporter": "^1.0.9",
    "solidity-coverage": "^0.8.4",
    "ts-node": "10.9.1",
    "typechain": "^8.3.1",
    "typescript": "5.2.2"
  }
}
